services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: simulador_educacional
      POSTGRES_USER: simulador
      POSTGRES_PASSWORD: simulador
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
